# Prise-en-main-jax-rs-todolist

=> JAX-RS → expose les endpoints REST
=> AuthFilter / Middleware → protège ces endpoints
=> TacheService → fait la logique métier et la transaction
=> JPA / Hibernate → interagit avec la base MySQL
